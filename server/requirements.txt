fastapi==0.115.0
uvicorn==0.30.6
jinja2==3.1.4
python-multipart==0.0.9

paddleocr==2.7.3
# IMPORTANT:
# You must install a compatible "paddlepaddle" wheel for macOS Apple Silicon.
# (Do NOT add paddlepaddle here blindly; install it separately as shown in README.)
